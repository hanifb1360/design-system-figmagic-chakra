import {
  colors,
  fontFamilies,
  fontSizes,
  fontWeights,
} from '../chakra/tokenIntegration';
import { extendTheme } from '@chakra-ui/react';

const theme = extendTheme({
  colors,
  fontFamilies,
  fontSizes,
  fontWeights,
});

export default theme;
